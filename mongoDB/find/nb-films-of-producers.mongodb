use("ct620574")

let filter = {
    producers: {
        firstName: "David",
        lastName: "Heyman",
    }
}

let nb_films = db.films.countDocuments(filter)

let {firstName, lastName} = filter.producers
print("Nombre de films produits par le realisateur \"" + firstName + " " + lastName + "\" : " + nb_films)
