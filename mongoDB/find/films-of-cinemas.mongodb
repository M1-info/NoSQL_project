use("ct620574")

let filter = {
    name : "Le Petit Rex"
}

let projection = {
    films: {title: 1},
    city: 1
}

let cinema = db.cinemas.findOne(filter, projection)

print("Liste des films diffusés dans let cinema " + filter.name + " situé à " + cinema.city + " : ")

cinema.films.forEach(film => { print("► " + film.title) })

