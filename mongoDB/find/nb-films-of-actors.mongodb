use("ct620574")

let filter = {
    actors: {
        firstName: "Daniel",
        lastName : "Radcliffe"
    }
}

let nb_films = db.films.countDocuments(filter)

let {firstName, lastName} = filter.actors
print("Nombre de films dans lesquels joue l'acteur \"" + firstName + " " + lastName + "\" : " + nb_films)
